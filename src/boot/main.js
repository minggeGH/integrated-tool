import { AMAP } from "@/utils/amapApi";
import { Wx } from "@/utils/wxApi";
// 系统初始化
export default async ({ app, router, store, urlPath }) => {
  // 注册高德API
  // const amap = new AMAP()
  // amap.load()
  // 初始化微信API
  // const wxApi = new Wx()
  // wxApi.getWx()
}


